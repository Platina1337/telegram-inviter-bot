# Telegram Inviter Bot

🤖 **Telegram Inviter Bot** - профессиональный инструмент для автоматического приглашения пользователей из одной Telegram группы в другую. Поддерживает два режима работы, ротацию сессий, гибкие настройки и полное управление через Telegram интерфейс.

## ✨ Основные возможности

### 🔄 Режимы инвайтинга

#### 1. **📋 Режим по списку участников** (`member_list`) - *рекомендуется*
- Просмотр полного списка участников группы-источника
- Инвайтинг в последовательном порядке
- Пропуск уже приглашенных пользователей
- Автоматическое продолжение после перезапуска

#### 2. **💬 Режим по сообщениям** (`message_based`)
- Анализ истории сообщений группы-источника
- Инвайтинг авторов сообщений (активных участников)
- Подходит для больших групп с ограниченным доступом к списку участников

### 🔐 Управление сессиями

- **Добавление сессий**: через CLI с интерактивной авторизацией
- **Назначение задач**: привязка сессий к конкретным типам задач
- **Ротация сессий**: автоматическое переключение при блокировках
- **Мониторинг**: статус всех сессий в реальном времени

### ⚙️ Гибкие настройки

#### Основные параметры:
- **Задержка между инвайтами**: 1-300 секунд (рекомендуется 30+)
- **Частота задержек**: после каждого N-го инвайта
- **Лимит приглашений**: максимальное количество инвайтов на задачу
- **Ротация сессий**: включение/отключение
- **Частота ротации**: после каждого N-го инвайта или только при ошибках

#### Защита от блокировок:
- Автоматическая обработка FloodWait
- Пропуск пользователей с закрытой приватностью
- Умная ротация сессий при критических ошибках
- Лимиты на количество инвайтов в час/день

## 🏗️ Архитектура

```
inviter/
├── bot/                    # Telegram бот (интерфейс управления)
│   ├── bot_main.py         # Основной файл бота
│   ├── handlers.py         # Обработчики команд и меню
│   ├── states.py           # FSM состояния и клавиатуры
│   ├── session_handlers.py # Управление сессиями
│   ├── api_client.py       # HTTP клиент для API
│   └── config.py           # Конфигурация бота
│
├── parser/                 # API сервер и воркер (ядро)
│   ├── main.py             # FastAPI приложение
│   ├── inviter_worker.py   # Логика инвайтинга
│   ├── session_manager.py  # Менеджер сессий
│   ├── database.py         # SQLite база данных
│   ├── add_session.py      # CLI для добавления сессий
│   └── config.py           # Конфигурация сервера
│
├── shared/                 # Общие модели
│   └── models.py           # Dataclasses (SessionMeta, InviteTask)
│
├── sessions/               # .session файлы Telegram
├── *.bat                   # Скрипты запуска (Windows)
├── start_parser.bat        # Запуск API сервера (Windows)
├── start_bot.bat           # Запуск Telegram бота (Windows)
├── add_session.bat         # Добавление сессии (Windows)
├── pyproject.toml          # Зависимости и метаданные
├── .env.example            # Пример конфигурации
└── README.md
```

## 🚀 Установка и запуск

### 📋 Системные требования

- **Python**: 3.8+ (рекомендуется 3.11+)
- **Оперативная память**: минимум 512MB
- **Диск**: минимум 100MB свободного места
- **Сеть**: стабильный интернет для работы с Telegram API

### 📦 Установка зависимостей

#### 1. Установка системных зависимостей

**Ubuntu/Debian (включая WSL):**
```bash
# Обновление пакетов
sudo apt update

# Установка Python venv и pip
sudo apt install python3-venv python3-pip python3-dev

# Для дополнительных библиотек (опционально)
sudo apt install build-essential libssl-dev libffi-dev
```

**CentOS/RHEL/Fedora:**
```bash
# CentOS/RHEL
sudo yum install python3-devel python3-pip
# или для новых версий
sudo dnf install python3-devel python3-pip

# Fedora
sudo dnf install python3-devel python3-pip
```

**macOS:**
```bash
# Установка через Homebrew (рекомендуется)
brew install python3

# Или обновление существующего Python
brew upgrade python3

# Установка зависимостей для компиляции
brew install openssl readline sqlite3 xz zlib
```

**Arch Linux:**
```bash
sudo pacman -S python python-pip python-virtualenv
```

#### 2. Клонирование и подготовка
```bash
# Перейти в директорию проекта
cd /path/to/project/inviter

# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
```

#### 2. Активация виртуального окружения

**Windows (cmd/PowerShell):**
```cmd
venv\Scripts\activate
```

**Windows (PowerShell):**
```powershell
venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

#### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

#### 4. Решение проблем с виртуальным окружением

**Если возникает ошибка "ensurepip is not available" (WSL/Debian/Ubuntu):**
```bash
# Установите python3-venv
sudo apt install python3.8-venv  # или python3.10-venv, в зависимости от версии Python

# Пересоздайте виртуальное окружение
python3 -m venv venv

# Активируйте
source venv/bin/activate
```

**Если виртуальное окружение не создается:**
```bash
# Проверьте версию Python
python3 --version

# Установите virtualenv как альтернативу
pip3 install virtualenv
virtualenv venv
source venv/bin/activate
```

**Проблемы с установкой зависимостей в виртуальном окружении:**
```bash
# Обновите pip в виртуальном окружении
source venv/bin/activate
pip install --upgrade pip

# Если возникают ошибки компиляции, установите build dependencies
# Ubuntu/Debian:
sudo apt install build-essential python3-dev

# CentOS/RHEL:
sudo yum groupinstall "Development Tools"
sudo yum install python3-devel

# macOS:
brew install openssl readline sqlite3 xz zlib
```

### ⚙️ Конфигурация

#### 1. Создание файла конфигурации
```bash
cp .env.example .env
```

#### 2. Заполнение .env файла
```env
# Токен Telegram бота (получить у @BotFather)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# API данные для сессий (получить на https://my.telegram.org)
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# Порт для API сервера (по умолчанию 8001)
API_PORT=8001

# Путь к базе данных (по умолчанию ./inviter.db)
DATABASE_PATH=./inviter.db

# Директория для сессий (по умолчанию ./sessions)
SESSIONS_DIR=./sessions
```

### 🔑 Получение API данных

1. Перейдите на сайт [https://my.telegram.org](https://my.telegram.org)
2. Авторизуйтесь с вашим номером телефона
3. Перейдите в раздел **API development tools**
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash`

### 🤖 Создание бота

1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен и вставьте в `.env` файл

## 🎯 Запуск приложения

### 📋 Что запускают скрипты:

- **`start_parser.bat`** (Windows) → Запуск FastAPI сервера для API (порт 8001)
- **`start_bot.bat`** (Windows) → Запуск Telegram бота для управления
- **`add_session.bat`** (Windows) → Интерактивное добавление Telegram сессии

На Linux/Mac используются эквивалентные Python команды напрямую.

### Windows

#### Вариант 1: Использование .bat скриптов (рекомендуется)
```cmd
# Запуск API сервера (терминал 1)
.\start_parser.bat

# Запуск бота (терминал 2)
.\start_bot.bat
```

#### Вариант 2: Ручной запуск с активацией окружения
```cmd
# Терминал 1 - API сервер
venv\Scripts\activate
python -m uvicorn parser.main:app --host 0.0.0.0 --port 8001 --reload

# Терминал 2 - Бот
venv\Scripts\activate
python -m bot.bot_main
```

#### Вариант 3: Ручной запуск без активации (если виртуальное окружение уже активно)
```cmd
# Терминал 1 - API сервер
python -m uvicorn parser.main:app --host 0.0.0.0 --port 8001 --reload

# Терминал 2 - Бот
python -m bot.bot_main
```

### Linux/Mac

#### Вариант 1: С виртуальным окружением (рекомендуется)
```bash
# Терминал 1 - API сервер
source venv/bin/activate
python -m uvicorn parser.main:app --host 0.0.0.0 --port 8001 --reload

# Терминал 2 - Бот
source venv/bin/activate
python -m bot.bot_main
```

#### Вариант 2: С загрузкой переменных окружения из .env файла
```bash
# Терминал 1 - API сервер
source venv/bin/activate
[ -f .env ] && export $(grep -v '^#' .env | xargs)
python -m uvicorn parser.main:app --host 0.0.0.0 --port 8001 --reload

# Терминал 2 - Бот
source venv/bin/activate
[ -f .env ] && export $(grep -v '^#' .env | xargs)
python -m bot.bot_main
```

### 🐳 Docker (опционально)

```bash
# Сборка образа
docker build -t telegram-inviter .

# Запуск контейнера
docker run -p 8001:8001 -v $(pwd)/sessions:/app/sessions telegram-inviter
```

## 🔐 Управление сессиями

### Добавление новой сессии

#### Windows:
```cmd
# Вариант 1: Через .bat скрипт
.\add_session.bat

# Вариант 2: Ручной запуск
venv\Scripts\activate
python -m parser.add_session
```

#### Linux/Mac:
```bash
# Вариант 1: С виртуальным окружением
source venv/bin/activate
python -m parser.add_session

# Вариант 2: С загрузкой переменных окружения
source venv/bin/activate
[ -f .env ] && export $(grep -v '^#' .env | xargs)
python -m parser.add_session
```

#### Процесс добавления:
1. Введите название сессии (alias)
2. Введите API ID
3. Введите API Hash
4. Введите номер телефона (+1234567890)
5. Введите код подтверждения из Telegram
6. При необходимости введите пароль двухфакторной аутентификации

### Управление сессиями через бота

1. Запустите бота командой `/start`
2. Перейдите в раздел **🔐 Сессии**
3. Выберите **"Показать сессии"** для просмотра всех сессий
4. Выберите **"Назначить сессию"** для управления сессией:
   - **👥 Инвайтинг** - привязка сессии к задачам инвайтинга
   - **🌐 Настроить прокси** - установка прокси для сессии
   - **🧪 Проверить прокси** - проверка работоспособности прокси
   - **🗑️ Удалить прокси** - удаление прокси из сессии
   - **📋 Копировать прокси** - копирование прокси на другие сессии

#### Настройка прокси для сессий

**Формат прокси:**
```
scheme://user:pass@host:port
scheme://host:port
```

**Поддерживаемые схемы:** `socks5`, `socks4`, `http`

**Пример:**
```
socks5://user123:pass456@192.168.1.100:1080
http://proxy.example.com:8080
```

**Проверка подключения:**
- После настройки прокси используйте кнопку **🧪 Проверить прокси**
- Система проверит подключение как с прокси, так и без него
- В случае ошибки будет показано подробное описание проблемы

## 👥 Использование бота

### Основной рабочий процесс

1. **Запуск бота**
   - Откройте бота в Telegram
   - Отправьте команду `/start`

2. **Настройка сессий**
   - Перейдите в **🔐 Сессии**
   - Добавьте хотя бы одну сессию через CLI
   - Назначьте сессию на задачу "Инвайтинг"

3. **Создание задачи инвайтинга**
   - Перейдите в **👥 Инвайтинг**
   - Выберите **группу-источник** (откуда брать пользователей)
   - Выберите **целевую группу** (куда приглашать)
   - Выберите **режим работы**:
     - 📋 **По списку участников** (рекомендуется)
     - 💬 **По сообщениям**
   - Настройте параметры

### Настройки инвайтинга

#### 📋 Режим "По списку участников"
```
• Группа-источник: должна быть доступна для чтения списка участников
• Целевая группа: бот должен иметь права администратора для приглашения
• Рекомендуется для групп до 10,000 участников
```

#### 💬 Режим "По сообщениям"
```
• Анализирует последние сообщения группы
• Приглашает только активных участников (авторов сообщений)
• Подходит для больших групп с ограниченным доступом к списку
• Может работать медленнее, но более безопасно
```

#### Параметры настройки:
- **Задержка между инвайтами**: 30-60 секунд (минимум!)
- **Частота задержек**: после каждого инвайта
- **Лимит приглашений**: установите разумный предел
- **Ротация сессий**: включите для надежности
- **Частота ротации**: после 10-20 инвайтов или только при ошибках

### Мониторинг задач

- Перейдите в **📊 Статус задач**
- Просмотрите активные, завершенные и неудачные задачи
- Для активных задач: пауза/возобновление/остановка

## 📊 Команды бота

### Основные команды:
- `/start` - Главное меню и начало работы
- `/sessions` - Управление сессиями (только администраторы)
- `/status` - Просмотр статуса всех задач

### Меню навигации:
- **🏠 Главное меню** - навигация по разделам
- **👥 Инвайтинг** - создание и настройка задач
- **📊 Статус задач** - мониторинг выполнения
- **🔐 Сессии** - управление Telegram сессиями

## 🛡️ Безопасность и защита

### ⚠️ Важные предупреждения

#### Ограничения Telegram:
- **FloodWait**: Не более 50 приглашений в час с одной сессии
- **Лимиты**: Максимум 100 приглашений в сутки на аккаунт
- **Блокировка**: Нарушение правил может привести к блокировке аккаунта

#### Рекомендации по безопасности:
- Используйте **несколько сессий** для распределения нагрузки
- Устанавливайте **длинные задержки** (30-60 секунд минимум)
- Не превышайте **50 приглашений в час** на сессию
- Регулярно **мониторьте статус задач**
- Используйте **ротацию сессий** для автоматического переключения

### 🔄 Ротация сессий

#### Принцип работы:
1. **Автоматическое переключение** при FloodWait или критических ошибках
2. **Проверка доступа** к группам перед переключением
3. **Исключение проблемных сессий** из текущей задачи
4. **Продолжение работы** с новой сессией без потери прогресса

#### Настройки ротации:
- **Отключена**: сессия работает до полной блокировки
- **Только при ошибках**: переключение только на FloodWait/блокировку
- **После N инвайтов**: принудительная ротация каждые N приглашений

### 📈 Статистика и мониторинг

#### Отслеживаемые метрики:
- Количество успешных приглашений
- Процент отказов и ошибок
- Время работы задач
- Статус каждой сессии

#### Уведомления:
- **Успешное завершение** задачи
- **Критические ошибки** с описанием проблемы
- **Предупреждения** о необходимости ротации
- **Статус сессий** при проблемах

## 🔧 Troubleshooting

### Распространенные проблемы

#### ❌ "ChannelPrivate" ошибка
```
Проблема: Нет доступа к приватной группе
Решение: Убедитесь, что сессия является участником группы
```

#### ❌ FloodWait
```
Проблема: Превышен лимит запросов
Решение: Увеличьте задержку, включите ротацию сессий
```

#### ❌ "Admin rights required"
```
Проблема: Бот не может приглашать в целевую группу
Решение: Назначьте сессию администратором с правами приглашения
```

#### ❌ Сессия не видит участников
```
Проблема: Список участников скрыт для не-администраторов
Решение: Используйте режим "По сообщениям" или назначьте администратора
```

### Логи и отладка

#### Просмотр логов:
```bash
# Логи API сервера
tail -f parser.log

# Логи бота
tail -f bot.log
```

#### Уровни логирования:
- `INFO`: Основные события и статусы
- `WARNING`: Предупреждения о проблемах
- `ERROR`: Критические ошибки
- `DEBUG`: Детальная информация для отладки

### Восстановление после сбоев

#### Принудительная остановка задач:
```bash
# Через API
curl -X POST http://localhost:8001/tasks/{task_id}/stop

# Или через бота в меню статуса задач
```

#### Очистка базы данных:
```bash
# Удаление всех задач
rm inviter.db
# Перезапуск приложения
```

## 🎛️ Дополнительные возможности

### История групп
- Автоматическое сохранение последних использованных групп
- Быстрый выбор из истории при создании новых задач
- Максимум 10 групп на пользователя

### Управление задачами
- **Пауза/возобновление** активных задач
- **Остановка** с сохранением прогресса
- **Просмотр истории** всех выполненных задач
- **Экспорт статистики** в удобном формате

### Система уведомлений
- **Telegram уведомления** о важных событиях
- **Статус задач** в реальном времени
- **Предупреждения** о проблемах с сессиями
- **Отчеты** о завершении работы

## 📝 API документация

### REST API endpoints:

#### Сессии:
- `GET /sessions` - список всех сессий
- `POST /sessions/add` - добавить сессию
- `DELETE /sessions/{alias}` - удалить сессию

#### Задачи:
- `GET /tasks` - список задач пользователя
- `POST /tasks` - создать задачу
- `GET /tasks/{id}` - статус задачи
- `POST /tasks/{id}/start` - запустить задачу
- `POST /tasks/{id}/stop` - остановить задачу

#### Группы:
- `GET /groups/{session_alias}/info` - информация о группе
- `GET /user/{user_id}/groups` - группы пользователя
- `GET /user/{user_id}/target_groups` - целевые группы пользователя

### Swagger документация:
После запуска API сервера откройте: `http://localhost:8001/docs`

## 🤝 Поддержка и обновления

### Полезные ссылки:
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Pyrogram документация](https://docs.pyrogram.org/)
- [FastAPI документация](https://fastapi.tiangolo.com/)

### Рекомендации по использованию:
- Начинайте с тестовых групп
- Постепенно увеличивайте нагрузку
- Регулярно проверяйте статус сессий
- Делайте бэкапы базы данных

---

**⚠️ Disclaimer**: Используйте инструмент ответственно. Соблюдайте правила Telegram и не нарушайте условия использования. Автор не несет ответственности за блокировки аккаунтов при неправильном использовании.
